spring:
  data:
    redis:
      host: redis
      port: 6379

management:
  zipkin:
    tracing:
      endpoint: http://scribble-zipkin:9411/api/v2/spans

eureka:
  client:
    service-url:
      defaultZone: http://discovery-server:8761/eureka/